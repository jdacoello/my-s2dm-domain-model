"""Represents an item available on the menu."""
type MenuItem {
    id: ID!
    price: Float 
        @range(min: 0) 
        @cardinality(min:1, max:1)
    kind: MenuItemKind!
}

union MenuItemKind = Pizza | Beverage | Dessert